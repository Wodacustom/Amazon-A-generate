# Compose 项目名，会影响容器、网络和数据卷的前缀。
COMPOSE_PROJECT_NAME=aplus-agent

# 宿主机访问端口：前端 http://localhost:5173，后端 http://localhost:8002。
FRONTEND_PORT=5173
BACKEND_PORT=8002

# PostgreSQL 初始化配置；如果改了用户、密码或库名，也要同步修改 DATABASE_URL。
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password
POSTGRES_DB=aplus_agent

# 后端连接容器内 PostgreSQL 的地址；主机名 postgres 来自 docker-compose 服务名。
# 注意需要使用 async SQLAlchemy 驱动前缀 postgresql+asyncpg。
DATABASE_URL=postgresql+asyncpg://postgres:password@postgres:5432/aplus_agent

# 后端连接容器内 Redis 的地址；主机名 redis 来自 docker-compose 服务名。
REDIS_URL=redis://redis:6379/0

# JWT 与模型配置密钥。生产环境必须替换默认值。
JWT_SECRET_KEY=dev-secret-change-me
JWT_EXPIRE_MINUTES=1440
MODEL_CONFIG_SECRET_KEY=dev-model-config-secret-change-me
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123456

# RustFS S3 兼容对象存储。
# RUSTFS_* 用于启动 RustFS 服务本身。
RUSTFS_ACCESS_KEY=rustfsadmin
RUSTFS_SECRET_KEY=rustfsadmin

# S3_* 用于后端通过 boto3 访问 RustFS。
S3_ENDPOINT_URL=http://rustfs:9000
S3_ACCESS_KEY=rustfsadmin
S3_SECRET_KEY=rustfsadmin
S3_REGION=us-east-1
S3_BUCKET=aplus-agent

# 前端允许访问后端的来源；FRONTEND_PORT 变化时这里也要同步调整。
ALLOWED_ORIGINS=["http://localhost:5173","http://127.0.0.1:5173"]

# 默认使用 mock，不填真实模型 key 也能本地启动。
# EMBEDDING_DIMENSIONS 需要和 database/schema_table.sql 中 vector(1536) 保持一致。
EMBEDDING_PROVIDER=mock
EMBEDDING_MODEL=mock-hash-v1
EMBEDDING_BASE_URL=
EMBEDDING_API_KEY=
EMBEDDING_DIMENSIONS=1536

# LLM/Embedding 真实模型按 OpenAI-compatible 接口配置 base URL 和 API key。
LLM_PROVIDER=mock
LLM_MODEL=mock-a-plus-v1
LLM_BASE_URL=
LLM_API_KEY=
LLM_TIMEOUT_SECONDS=60
LLM_TEMPERATURE=0.2
